# API-only deps. Engine deps (soundfile, numpy) come from
# services/python-audio/requirements.txt (installed separately in the Dockerfile).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
