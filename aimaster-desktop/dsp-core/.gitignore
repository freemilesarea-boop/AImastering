/target
Cargo.lock
**/*.rs.bk
# wasm-bindgen output — generated by `scripts/build-wasm-bindings.sh`.
# We commit the canonical output to `packages/dsp-wasm/pkg/` instead of
# this temporary build location so consumers don't need wasm-bindgen-cli.
crates/loui-dsp-wasm/pkg/
