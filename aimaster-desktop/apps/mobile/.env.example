# Mastering runs ENTIRELY on-device (Web Audio API) — there is NO server to
# configure. No Render URL / API key is needed or used.
#
# Surfaced in the header / about (optional).
VITE_APP_VERSION=1.0.0
