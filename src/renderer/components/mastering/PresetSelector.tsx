/**
 * 마스터링 프리셋 선택 컴포넌트 (legacy alias)
 *
 * v3 에서 MasteringPresets 로 대체되었음. 외부에서 PresetSelector 이름으로
 * 참조하던 코드를 깨지 않기 위해 alias 로 유지한다.
 */
export { MasteringPresets as PresetSelector } from './MasteringPresets'
